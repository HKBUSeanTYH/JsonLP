#ifndef TOKEN_TYPE_DEF
#define TOKEN_TYPE_DEF

enum class TokenType {
    LEFT_BRACE,
    RIGHT_BRACE,
    LEFT_BRACKET,
    RIGHT_BRACKET,
    COLON,
    COMMA,
    STRING,
    NUMBER,
    BOOLEAN,
    NULL
};

#endif